from __future__ import annotations

from fastapi import Request
from starlette.middleware.base import BaseHTTPMiddleware


# Placeholder if later switching from middleware to dependency-based integration.
# Current Phase 5 uses middleware only, per requirements.

